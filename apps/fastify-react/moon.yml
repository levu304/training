$schema: 'https://moonrepo.dev/schemas/project.json'

platform: 'node'

language: 'typescript'

stack: 'frontend'

type: 'application'
    
tasks:
  dev:
    command: "pnpm run dev"
    options:
      cache: false
    local: true

  build:
    command: "pnpm run build"
    deps:
      - '^:build'