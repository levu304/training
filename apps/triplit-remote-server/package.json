{
  "name": "triplit-remote-server",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "bun build --outdir dist --target node ./src/index.ts",
    "compile": "bun build --compile --minify --outfile build/server ./dist/index.js"
  },
  "devDependencies": {
    "@types/node": "22.13.14",
    "typescript": "^5"
  },
  "dependencies": {
    "@triplit/server": "^1.1.8",
    "lmdb": "^3.4.2"
  }
}
