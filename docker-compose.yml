services:
  waku:
    build:
      context: .
      dockerfile: ./apps/waku-project/Dockerfile
    container_name: waku
    restart: unless-stopped
    environment:
      NODE_ENV: production
    ports:
      - 8080:8080
